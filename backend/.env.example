# Database
DATABASE_URL=sqlite:///./kingo.db

# 데이터베이스 초기화 옵션 (서버 재시작 시 DB 리셋 여부)
# true: 서버 시작 시마다 모든 테이블 삭제 후 재생성 (개발/테스트용, 데이터 손실)
# false: 테이블이 없을 때만 생성 (프로덕션, 데이터 보존) - 기본값
RESET_DB_ON_STARTUP=false

# Security
SECRET_KEY=your-secret-key-change-in-production-min-32-chars

# CORS - 쉼표로 구분된 origin 목록 (선택)
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Claude AI API Key (필수 - https://console.anthropic.com/에서 발급)
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Alpha Vantage API Key (필수 - https://www.alphavantage.co/support/#api-key에서 발급)
ALPHA_VANTAGE_API_KEY=your-alpha-vantage-api-key-here
